import * as path from 'path';

export const BASE_URL = 'https://zapisy.ii.uni.wroc.pl';
export const BATCH_SIZE = 5;
export const MAX_NEWS_ITEMS = 100;
export const OUTPUT_DIR = path.join(process.cwd(), '../data/raw');